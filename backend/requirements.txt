# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
aiomysql==0.2.0
alembic==1.13.0

# Authentication
python-jose[cryptography]==3.3.0
# cryptography is required for MySQL 8 caching_sha2_password auth via
# aiomysql/pymysql. Pin it explicitly - it is easy to end up without it if
# python-jose is installed without the [cryptography] extra.
cryptography>=42.0.0
bcrypt>=4.0.1
python-dotenv==1.0.0

# Validation
pydantic==2.5.2
pydantic-settings==2.1.0
email-validator==2.1.0

# File handling
aiofiles==23.2.1
python-magic==0.4.27

# CORS
fastapi-cors==0.0.6

# Rate limiting
slowapi==0.1.9

# Logging
loguru==0.7.2

# AI Assessment (Groq)
groq>=1.6.0

# Testing
pytest==7.4.3
pytest-asyncio==0.23.2
httpx==0.25.2

# Development
black==23.12.0
flake8==6.1.0
mypy==1.7.1

# Optional: Redis for caching
# redis==5.0.1
# aioredis==2.0.1

# Optional: Email support
# jinja2==3.1.2
# emails==0.6